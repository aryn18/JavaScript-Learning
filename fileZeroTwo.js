"use strict";

// Intro to variables

// variables can store some information
// we can use that information later
// we can change that information later

// declare a variable

var firstName = 'John';
console.log(firstName);

// change value

firstName = "David";
console.log(firstName);

// if we change the variable data we don't used var keyword if we use the var keyword javaScript comiler thinks user developer makes a new variable.